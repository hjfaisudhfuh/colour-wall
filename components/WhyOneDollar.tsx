export function WhyOneDollar() {
  return (
    <section className="mx-auto my-16 max-w-2xl px-4 text-center">
      <h2 className="font-serif text-3xl text-zinc-900 sm:text-4xl">
        Why $1?
      </h2>
      <p className="mt-4 text-base leading-relaxed text-zinc-700">
        $1 keeps the wall intentional, prevents spam, and makes each square
        feel chosen. Claim one square, or build something bigger over time.
      </p>
    </section>
  );
}
